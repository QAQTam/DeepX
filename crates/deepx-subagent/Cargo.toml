[package]
name = "deepx-subagent"
version.workspace = true
edition.workspace = true
description = "DeepX subagent tool — spawn sub-agents for focused tasks"

[dependencies]
deepx-workspace = { path = "../deepx-workspace" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"

[lints]
workspace = true
